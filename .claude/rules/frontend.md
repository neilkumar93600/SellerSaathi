# Frontend Rules

- Use Next.js 15 App Router.
- Prefer Server Components; use `"use client"` only when necessary (state, hooks).
- Use Tailwind CSS v4 for styling.
- Use shadcn/ui for components.
- Ensure responsive design (375px+).
- Support multiple languages (UI & Output).