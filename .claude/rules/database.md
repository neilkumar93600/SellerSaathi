# Database Rules

- Use Supabase (PostgreSQL).
- Enforce Row Level Security (RLS) on all tables.
- No client-side secrets.
- Use Drizzle ORM (if applicable) or Supabase client for queries.
- Track credits effectively (Free vs Pro vs Agency).