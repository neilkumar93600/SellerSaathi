# API Rules

- Use Next.js Route Handlers (`/api/*`).
- Secure all protected routes (dashboard, AI tools, settings).
- Razorpay webhook verification is mandatory.
- Implement streaming for AI responses (DeepSeek).